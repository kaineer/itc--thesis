// reverse, toReversed, sort, toSorted

// reverse - переворачивает массив и возвращает его
const list = [1, 2, 3];
console.log(list.reverse());
console.log(list);

// toReversed - возвращает перевернутый массив, но не трогает исходный
// const fixedList = [1, 2, 3];
// console.log(fixedList.toReversed());
// console.log(fixedList);

// const unsorted = [4, 2, 3, 1];
// console.log(unsorted.sort());
// console.log(unsorted);

// const fixedUnsorted = [4, 2, 3, 1];
// console.log(fixedUnsorted.toSorted());
// console.log(fixedUnsorted);
